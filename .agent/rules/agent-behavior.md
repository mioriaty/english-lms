# Agent Behavior Rules

## Browser

**NEVER open URLs in a browser.** Do not use any browser-opening tools, `browser_subagent`, or commands that launch a browser window (e.g. `open`, `start`, `xdg-open`). This is a hard rule with no exceptions, even for verification purposes.
