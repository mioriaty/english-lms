# Workspace Rules

## Output Directory for Plans and Brainstorms
All project plans (generated by `/plan`) and brainstorm explorations (generated by `/brainstorm`) MUST be saved in the `plans/` directory at the root of the project, instead of the project root or chat output.
